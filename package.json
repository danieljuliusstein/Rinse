{
  "name": "rinse",
  "private": true,
  "description": "Rinse monorepo — desk, mobile, api, pocketbase, packages/core",
  "scripts": {
    "desk": "npm run dev --prefix rinse-desk",
    "mobile": "npm run start --prefix rinse-mobile",
    "api": "npm run dev --prefix rinse-api",
    "api:build": "npm run build --prefix rinse-api"
  }
}
