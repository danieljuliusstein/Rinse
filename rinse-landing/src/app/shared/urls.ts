/**
 * Authenticated product — Desktop CRM (Rinse Desk).
 * Set this once Desk is deployed; Sign in stays hidden while unset.
 */
export const DESK_URL: string | null = "https://desk.rinsehq.com";

export const SIGN_IN_URL = DESK_URL;
