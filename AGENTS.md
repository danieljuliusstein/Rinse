# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

## Native is primary

Operator features ship in **`apps/mobile/` only**. `apps/api` is the API + customer book/portal/admin — not a parallel operator UI.

Before UI work, read existing native screens and `src/theme/tokens.ts`. Workspace rule: `native-pwa-reference`.
