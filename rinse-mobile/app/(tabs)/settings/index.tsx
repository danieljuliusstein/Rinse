import SettingsHubScreen from '../../settings/index'

/** Settings hub as a bottom-tab root (keeps dock visible). */
export default function TabSettingsScreen() {
  return <SettingsHubScreen tabRoot />
}
